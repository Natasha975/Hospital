<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="МедДосье.TelemedicinePage">
    
    <ContentPage.Content>
        <StackLayout>
            <Grid Padding="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ImageButton Source="user_icon.png" WidthRequest="32" HeightRequest="32"
                       Grid.Column="2" BackgroundColor="Transparent" Clicked="OnProfileClicked"/>
            </Grid>

            <Frame CornerRadius="10" Padding="15" Margin="20,40,20,10" BackgroundColor="#EBF5FF" BorderColor="#EBF5FF">

                <StackLayout Orientation="Horizontal">
                    <StackLayout VerticalOptions="Center">
                        <Label Text="Не нашли нужную услугу?" FontSize="16" TextColor="Black"/>
                        <Label Text="Свяжитесь с нами по телефону 999 999" FontSize="14" TextColor="Black"/>
                    </StackLayout>

                </StackLayout>
            </Frame>

            <Label Text="Специалисты" FontSize="18" TextColor="Black" Margin="20,10"/>

            <CollectionView ItemsSource="{Binding Specialities}" SelectionMode="Single" SelectionChanged="CollectionView_SelectionChanged">

                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="3" VerticalItemSpacing="10" HorizontalItemSpacing="10"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>

                    <DataTemplate>
                        <Frame CornerRadius="10" Padding="10" BackgroundColor="White" BorderColor="#F5F5F5" HasShadow="False" Margin="2" HeightRequest="120">

                            <StackLayout>
                                <Label Text="{Binding Name}" FontSize="16" TextColor="Black" HorizontalOptions="Center"/>
                                <Label Text="{Binding Price}" FontSize="14" TextColor="Gray" HorizontalOptions="Center"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>